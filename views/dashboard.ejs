<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../css/styles.css" ><!-- Import debe ser "../algo" para este caso -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
    <title>Lista de Rutas</title>
</head>
<body>
    <div class="name"> 
        <img src="../img/noFondologoCucutaRutas.png" class="imagenLogo" alt=""> <!-- Import debe ser "../algo" para este caso -->
            <div class="content">   
                <h2>Rutas Asociadas</h2>
                <div class="mi-boton"> 
                    <a href="/logout"class="btn btn-danger form-control">Cerrar Sesión</a>
                </div>
                <div class="mi-boton-volver"> 
                    <a href="asignar-rutas.html"class="btn btn-success form-control">Asignar Rutas</a>
                </div>
                <div class="mi-boton-agregar"> 
                    <a href="/admin/add"class="btn btn-success form-control">Agregar Rutas</a>
                </div>
               
            </div> 
    </div> 

    <div class="info">
        <p class="titulo_sección"> Lista de todas las rutas asociadas disponibles</p>
        <div class="list-group">
            <% if (rutas) { %>
                <% rutas.forEach(function(ruta){ %>
                    <a href="#" class="list-group-item list-group-item-action" aria-current="true">
                        <div class="d-flex w-100 justify-content-between">
                        <h5 class="mb-1"><%= ruta.nombre %></h5>
                        </div>
                        <p class="mb-1">Duracion: <%= ruta.duracion %></p>
                        <p class="mb-1">Hora de inicio: <%= ruta.horaInicio %></p>
                        <p class="mb-1">Hora de fin: <%= ruta.horaFin %></p>
                        <p class="mb-1">Frecuencia: <%= ruta.frecuencia %></p>
                    </a>
                <% }); %>
            <% } %>
        </div>
        <div class="mb-3">
            <a href="editar-rutas.html" class="btn btn-success form-control">Editar</a>
          </div>
    </div>     

</body>
</html>
